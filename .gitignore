# Terraform
.terraform/
.terraform.lock.hcl
terraform/**/*.tfstate
terraform/**/*.tfstate.*
terraform/**/lambda.zip
terraform/**/crash.log

# Lambda organize bundle (generato da build.py prima di terraform apply)
lambda_organize/build/
lambda_notion/build/

# ZIP Terraform (generati localmente)
terraform/organize_lambda.zip
terraform/notion_lambda.zip

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Local secrets (never commit)
secret.json
terraform/terraform.tfvars
*.pem
